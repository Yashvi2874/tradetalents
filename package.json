{
  "name": "tradetalents",
  "version": "1.0.0",
  "description": "A skill exchange platform for university students",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mern", "skill-exchange", "education", "react", "nodejs"],
  "author": "TradeTalents Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}